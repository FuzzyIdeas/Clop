## Features

- Press `Space` while hovering a floating result to QuickLook the file
  > Works just like in Finder, replacing the old `Shift` trigger

## Fixes

- *Optimised file location* is respected when re-optimising from the floating result
- CLI `--copy` should not copy the path to clipboard as expected
- The close (x) button should dismiss the result as expected
